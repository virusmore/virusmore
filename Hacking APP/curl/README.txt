Kurl – kill web-auth via curl

APP Name:  kurl —— kill http-auth via curl

Author: 閦蠱(virusmore)

Description:
Kurl helps with online brute-force attack targeting web-auth based WEB server, which may be Apache, IIS, nginx or tomcat on *nix or Windows.
This tool is very simple and light-weight. It can run on almost all *nix systems and *nix Simulator on Android, iOS, MacOS and Windows.
Implementation:

Test Environment:
Windows:
——Windows Srv 2012 R2
——IIS
——HTTP-Auth: BASIC, DIGEST
Linux:
——CentOS 8
——Apache
——HTTP-Auth: BASIC, DIGEST
